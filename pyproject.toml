# Verdaca — uv workspace root.
# Virtual workspace; the repo root itself is not a Python project. All code
# lives in workspace members listed below.
#
# Authored 2026-04-29 as part of Stage 9.4.2-pre bootstrap sub-stage.
# Charter: F3=(d) effective; pin reproducible env going forward.
# Bootstrap memo: _bmad-output/planning-artifacts/Verdaca/stage9.4.2-pre-bootstrap-memo.md

[tool.uv.workspace]
members = [
    "ports",
    "adapters/beads",
    "adapters/tonl",
    "adapters/in_tree_compaction_stub",
    "adapters/pi_mono_native",
    "adapters/litellm",
    "adapters/mcp_server",
    "adapters/channels/teams",
    "adapters/channels/slack",
    "adapters/mem0",
    "adapters/letta",
    "adapters/llmlingua",
    "tests",
    "kernel/compression",
    "kernel/mac",
    "kernel/gateway",
    "kernel/memory",
    "kernel/pi-mono/src",
    "kernel/runtime",
    "kernel/session_index",
    "kernel/studio",
]

[tool.importlinter]
root_packages = ["praxis", "adapters", "kernel", "shell"]

[[tool.importlinter.contracts]]
id = "spike-snapshot-not-importable"
name = "spike-snapshot-not-importable"
type = "forbidden"
source_modules = ["praxis", "adapters", "kernel", "shell"]
forbidden_modules = ["tests.reference.*"]

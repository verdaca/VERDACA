# Session Handoff — Stage 9.4.2-internal step-7 close (TONL Serialization MAC-T catalog)

**Sub-stage:** 9.4.2-internal step 7 — TONL adapter MAC-T authoring per `test-strategy.md` v0.2 §2.2.2 (Serialization Port (ADR-2) — 12 MAC-Ts)
**Status:** **CLOSED** — substance commit `1ea7ac6` (2026-05-01)
**Author:** Executor (Claude Code CLI session, model `claude-opus-4-7[1m]`) under advisor review
**Companion artifact:** None — single-commit charter; no separate close memo (this close-handoff is the only close artifact)

---

## §1. Scope summary

**Charter close (2026-05-01).** Stage 9.4.2-internal step 7 lands the 12-MAC-T contract test catalog at `tests/src/praxis/contract_tests/ports/test_serialization_contract.py` per `test-strategy.md` v0.2 §2.2.2 (Serialization Port (ADR-2)). The catalog comprises 10 binding TONL adapter tests (`M-T-SER-{ENCODE,DECODE}-TONL-01`, `M-T-SER-EVO-{01..04}`, `M-T-SER-FUZZ-{01..03}`, `M-T-SER-EXTRA-FORBID-01`) and 2 deferred-substrate `pytest.skip` skeletons for the msgspec adapter (`M-T-SER-{ENCODE,DECODE}-MSGSPEC-01`), preserving the §2.2.2 12-MAC-T count while honoring the ADR-9.2-V2 v0.2 Stage-10 substrate-deferral. `@pytest.mark.no_waiver` applied to `M-T-SER-EVO-01` (allow-list entry #21 — `SchemaEvolutionFailure`) and `M-T-SER-FUZZ-01` (allow-list entry #22 — `RoundtripDriftDetected`) per §6.1 ratification (test-strategy.md:645 directive). EVO-04's structural-reading docstring pin (literal substring `"Structural reading per advisor 9.4.2-internal step-7 disposition"`) keeps the no-executable-forward-migration-chain caveat grep-discoverable for Stage-10 revision. Single substance commit at `1ea7ac6`; no corrigendum.

**Debt-acknowledgment scope.** The §F (X) marker application surfaced four F-docket-class findings during halt-points #1 and #2.5: marker registration absent in `tests/pyproject.toml` (F11), MAC meta-test 16-entry allow-list non-overlap with verdaca/stage9 §6.1 entries #21/#22 (F12), the verdaca/stage9 22-entry enforcer being paper-only (F13), and a handover §"Pre-execution preload" Item-4 paraphrase divergence vs bootstrap memo §5 verbatim (F14). Step 7 honored hard halt-class boundaries throughout — no edits to any `pyproject.toml`, `conftest.py`, meta-test, marker registration, or enforcer file. F11 / F12 / F13 dispositioned per advisor halt-point #2.5: marker application proceeds per §6.1 ratification, enforcement-gap captured here for next-Amelia-stage resolution. Item-4 paraphrase classified as non-defect F6-precedent shape (handover-paraphrase-vs-binding-source mismatch).

**Outcome.** Repo at `1ea7ac6` (HEAD on `main`) carries the 12-MAC-T catalog tracked + green; pytest baseline at the `tests/src/praxis/contract_tests/ports/` scope is 22 collected / 20 passed / 2 skipped / 2 warnings. `§9.C` runtime contract still holds (`isinstance(TONLAdapter(), SerializationPort) == True`); no collateral damage to the 10-test `test_versioned_state_contract.py` precedent. Working tree clean except for the pre-existing `docs/pipeline-stage9.md` modification (parked per `session-handoff-20260430-stage9.4.2-pre-close.md` §6).

---

## §2. Timeline (commit ledger)

| SHA | Role | Description |
|---|---|---|
| `1ea7ac6` | Substance close (2026-05-01) | TONL Serialization port 12-MAC-T contract test catalog — 1 file added (347 insertions); 10 binding TONL adapter tests + 2 deferred-substrate `pytest.skip` MSGSPEC skeletons + `@pytest.mark.no_waiver` on `M-T-SER-EVO-01` and `M-T-SER-FUZZ-01` per §6.1 entries #21 / #22 |

Branch state on `main`: `04a4992 → 1ea7ac6` (close-handoff commit shape pending halt-point #7 disposition).

---

## §3. F-docket

### §3.0. Status index

| ID | Title | Disposition |
|---|---|---|
| F11 | `tests/pyproject.toml` lacks `no_waiver` marker registration | Open-deferred — paired with verdaca/stage9 enforcer landing at next Amelia stage |
| F12 | MAC meta-test 16-entry allow-list does not span verdaca/stage9 §6.1 entries #21 / #22 | Open-deferred — bounded scope-fence pending verdaca/stage9 enforcer or MAC enforcer cross-stage extension |
| F13 | Verdaca/stage9 §6.1 22-entry `no_waiver` enforcer is paper-only | Open-deferred — Amelia-stage authoring task per `test-strategy.md:487` spec annotation |
| F14 | Handover §"Pre-execution preload" Item-4 "10-element" paraphrase vs bootstrap memo §5 "10-path" verbatim | Classified — non-defect paraphrase drift (F6-precedent shape) |

### §3.1. F11 — `tests/pyproject.toml` lacks `no_waiver` marker registration

**Body.** Halt-point #2.5 verification surfaced that `tests/pyproject.toml` (the binding pytest config for the `praxis-contract-tests` workspace member, where `test_serialization_contract.py` lives) carries only `pythonpath = ["src", "."]` under `[tool.pytest.ini_options]`; grep for `no_waiver|markers|strict-markers|filterwarnings` against the file returns zero matches. By contrast, `kernel/mac/pyproject.toml:24,34,37` and `kernel/runtime/pyproject.toml:37,43,49–51` both register the marker — `kernel/mac` adding `--strict-markers` at line 24 (alongside `markers = [` at line 34 and the `no_waiver` description at line 37), and `kernel/runtime` escalating `PytestUnknownMarkWarning` to error at lines 49–51 (alongside `markers = [` at line 37 and the `no_waiver` description at line 43). Under `pytest tests/…` post-`1ea7ac6`, two `PytestUnknownMarkWarning` messages surface at collection — one per `@pytest.mark.no_waiver` decorator on `M-T-SER-EVO-01` and `M-T-SER-FUZZ-01` — non-fatal because the `tests/` member's pytest config carries no `filterwarnings` escalation matching the runtime member's pattern.

**Disposition.** Open-deferred. Per advisor halt-point #2.5 (P3 with refinements), `tests/pyproject.toml` registration is a halt-class boundary — pytest-config edits sit adjacent to the meta-test territory the §F (X) directive depends on, and cherry-picking the `no_waiver` registration alone in step 7 would constitute incomplete meta-test-adjacent scope expansion. The right registration is a paired landing with the verdaca/stage9 22-entry enforcer (F13); the marker should be registered in the scope where its enforcement lands, not as a free-standing config edit. The `PytestUnknownMarkWarning` warnings are acceptable under step-7 invocation patterns (non-fatal, no escalation in `tests/pyproject.toml`); the commit-body acknowledgment at `1ea7ac6` plus this F-docket make the gap grep-discoverable downstream.

**Forward action.** Pair with F13 at the next Amelia stage that opens pytest-config / meta-test scope. Coupled landing: register `no_waiver` in `tests/pyproject.toml` `[tool.pytest.ini_options].markers` (description-shape precedent `kernel/runtime/pyproject.toml:43`); decide whether to add `--strict-markers` (precedent `kernel/mac/pyproject.toml:24`); decide whether to escalate via `filterwarnings = ["error::pytest.PytestUnknownMarkWarning"]` (precedent `kernel/runtime/pyproject.toml:49–51`); land the enforcer at `tests/static/verdaca/test_no_waiver_inventory.py` per `test-strategy.md:487` annotation. Do NOT register on isolated initiative under any future step-7-shape charter; the coupling-with-enforcer is the disposition.

### §3.2. F12 — MAC meta-test 16-entry allow-list does not span verdaca/stage9 §6.1 entries #21 / #22

**Body.** `kernel/mac/tests/static/runtime/test_no_waiver_inventory.py` enforces a 16-entry `NO_WAIVER_ALLOWLIST` (frozenset declared at lines 70–103) transcribed verbatim from `mac/test-strategy.md v0.3 §13.3.2` (per the file's docstring at lines 104–105: "The 16 ratified `no_waiver` test nodeids per v0.3 §13.3.2. **RATIFIED 2026-04-14 via §13.3.3.1 Option 1.** Do not modify."). The 16 entries comprise 1 cross-tree Runtime ratification + 2 R53 OTEL exporter entries + 10 MAC gate calibration / structural tests + 2 MAC asymmetry/adversarial deterministic carriers + 1 self-reference (entry #16, the meta-test's own nodeid). The verdaca/stage9 §6.1 22-entry allow-list at `_bmad-output/implementation-artifacts/verdaca/stage9/test-strategy.md` adds entries #17–#22 in a separate document scope; entries #21 (`M-T-SER-EVO-01` carrying `SchemaEvolutionFailure`, line 469) and #22 (`M-T-SER-FUZZ-01` carrying `RoundtripDriftDetected`, line 470) are the two SER entries landed via §F (X) at `1ea7ac6` and are NOT present in the MAC enforcer's 16-entry list. The two scopes are deliberately distinct documents (MAC monorepo vs verdaca/stage9), not a single allow-list awaiting reconciliation.

**Disposition.** Open-deferred — bounded scope-fence pending verdaca/stage9 enforcer or MAC enforcer cross-stage extension. The MAC enforcer computes `unauthorized = collected_no_waiver_ids - LOCAL_ALLOWLIST` at line 163 and, if the set is non-empty, raises `AssertionError` (`raise` statement at line 181) under the header `"OQ-TS-9 allow-list meta-test FAILED:"` (line 182); the error body assembled at lines 167–172 carries the section header `"Unauthorized no_waiver tests (not in 16-entry allow-list per v0.3 §13.3.2)"` (lines 169–170) followed by the sorted offending nodeids. Combined-tree pytest invocation collecting both `kernel/mac/tests/` and `tests/src/praxis/contract_tests/ports/test_serialization_contract.py` would surface the SER nodeids in `unauthorized` — a real latent failure. The risk is bounded structurally: under MAC-only `pytest kernel/mac/tests/` the SER tests are not collected and the MAC enforcer is blind to them; under `tests/`-only `pytest tests/` the MAC enforcer is not collected at all; no current invocation pattern at 9.4.2-internal scope (no root-level `[tool.pytest.ini_options]`, no CI configuration spanning both monorepos) requires combined-tree invocation. The `1ea7ac6` substance landed cleanly under `tests/`-only invocation per halt-point #5 §D evidence (22 collected / 20 passed / 2 skipped / 2 warnings). Per advisor halt-point #2.5 (P3 with refinements), neither extending the MAC enforcer's allow-list nor authoring a parallel enforcer is in step-7 scope — both are meta-test territory and require coupled landing with F11 / F13.

**Forward action.** Pair with F11 + F13 at the next Amelia stage that opens meta-test / pytest-config scope. Two top-level resolution shapes are available: (i) **verdaca/stage9 enforcer landing** at `tests/static/verdaca/test_no_waiver_inventory.py` per `test-strategy.md:487` annotation, enforcing the 22-entry §6.1 allow-list within the verdaca/stage9 scope and leaving the MAC enforcer untouched (clean separation by document scope; combined-tree invocation still fails the MAC enforcer until path (ii) also lands or the MAC allow-list is extended); (ii) **MAC enforcer cross-stage extension** to span the 22-entry §6.1 set, requiring a `mac/test-strategy.md` v0.4 amendment + Murat re-ratification per the modification discipline at the meta-test source lines 55–59 ("DO NOT add, remove, or reorder entries without explicit team-lead ratification + a v0.4 test-strategy amendment + Murat re-ratification"). **Underlying gap:** the architectural call is whether Verdaca's `no_waiver` enforcement should be one-allow-list-per-document-scope (verdaca/stage9 enforcer + MAC enforcer in parallel, each scoped to its own monorepo, combined-tree invocation never expected outside deployment-time co-installation), one-allow-list-spanning-all-document-scopes (MAC enforcer extends to cover entries #17–#22 cross-stage, single-source-of-truth at the cost of cross-document write coupling), or cross-tree-deployment-time-only (each scope's enforcer enforces its own subset locally, full union only enforced at deployment-time pytest collection across both monorepos per the `CROSS_TREE_ALLOWLIST` precedent shape at lines 108–121). The choice belongs to the Amelia stage that owns the coupled F11 + F12 + F13 landing; advisor adjudication required.

### §3.3. F13 — Verdaca/stage9 §6.1 22-entry `no_waiver` enforcer is paper-only

**Body.** `_bmad-output/implementation-artifacts/verdaca/stage9/test-strategy.md` §6.1 (`### 6.1 Allow-List Verbatim (22 entries, binding)` at line 442; entries enumerated at lines 446–470 — 16 preserved verbatim from Stage 5.2 v0.3 at lines 446–461 + 6 minted at Stage 9.3 at lines 465–470) ratifies a 22-entry `no_waiver` allow-list whose structural enforcer is spec-only at present. The spec scaffolding lives at §6.3 (`### 6.3 Allow-List Meta-Test (Self-Enclosure)` at line 482), naming the future enforcer file at `tests/static/verdaca/test_no_waiver_inventory.py` (line 487 verbatim: `# tests/static/verdaca/test_no_waiver_inventory.py — spec only; Amelia 9.4 implements`) and providing a `def test_no_waiver_allowlist_is_binding():` function skeleton at line 488 onward that the future enforcer is expected to implement. The named file does not exist on disk: `test -d tests/static/verdaca/` returns `ABSENT`; `ls tests/` shows `.pytest_cache`, `pyproject.toml`, and `src/` only, with no `static/` subdirectory at all. The §F (X) marker application landed at `1ea7ac6` carries `@pytest.mark.no_waiver` on `M-T-SER-EVO-01` (allow-list entry #21, line 469) and `M-T-SER-FUZZ-01` (allow-list entry #22, line 470) per the binding directive at line 645 — but no in-tree code currently enforces the 22-entry list against pytest collection in the verdaca/stage9 scope.

**Disposition.** Open-deferred — Amelia-stage authoring task per the `Amelia 9.4 implements` spec annotation at line 487. Per advisor halt-point #2.5 (P3 with refinements), authoring the enforcer within step-7 charter scope would constitute meta-test territory expansion; the halt-class boundary held throughout step 7 (no enforcer file created, no `tests/static/` directory created, no edits to any meta-test). The marker application at `1ea7ac6` is structurally meaningful even without the enforcer landing — the binding directive at line 645 is the authority for marker placement and §6.1 ratification is the binding source for which entries the marker covers; the enforcer makes the directive's scope mechanically falsifiable but is not a precondition for marker correctness. F13 is paired with F11 (the marker registration in `tests/pyproject.toml`) at the next Amelia stage's coupled landing — neither component lands meaningfully alone.

**Forward action.** Next Amelia stage authors `tests/static/verdaca/test_no_waiver_inventory.py` against the §6.1 22-entry allow-list. Coupled landing scope: (a) create the `tests/static/verdaca/` directory; (b) author the enforcer file with a `NO_WAIVER_ALLOWLIST: frozenset[str]` constant transcribing the 22 entries verbatim from §6.1 lines 446–470 (with `M-T-*` MAC-T IDs translated to actual pytest nodeids per the test-file paths); (c) implement the bilateral-enforcement check (`unauthorized = collected - allowlist; missing = allowlist - collected`) per the §6.3 skeleton at line 488 onward; (d) decide cross-tree handling per F12 advisor adjudication (parallel-enforcers vs. cross-stage extension vs. deployment-time-only); (e) land in the same coupled scope as F11 (`tests/pyproject.toml` marker registration), so that the marker registration and its enforcement are structurally coherent. The MAC enforcer at `kernel/mac/tests/static/runtime/test_no_waiver_inventory.py:70–103` is referenced as a *structural-precedent shape* only — the MAC allow-list contents (16 entries sourced from `mac/test-strategy.md v0.3 §13.3.2`, a separate document scope) MUST NOT be transcribed into the verdaca/stage9 enforcer, since cross-document allow-list transcription would conflate distinct ratification scopes. Do NOT author the enforcer on isolated initiative; F11 + F13 coupled landing is the disposition.

### §3.4. F14 — Handover §"Pre-execution preload" Item-4 "10-element" paraphrase vs bootstrap memo §5 "10-path" verbatim

**Body.** The executor handover at `_bmad-output/planning-artifacts/Verdaca/session-handoff-20260501-stage9.4.2-internal-executor.md` line 56 directs (verbatim): *"4. **Workspace sanity** — re-run namespace-package import smoke: `python -c "import praxis.ports.serialization; import praxis.adapters.tonl.adapter; import praxis; print(len(praxis.__path__))"`. Confirm 10-element `praxis.__path__` aggregation. (Note F10 caveat in close-handoff §3.5 about `praxis.kernel` asymmetry — surface but do NOT auto-resolve.)"* The bootstrap memo §5 obs 1 at `_bmad-output/planning-artifacts/Verdaca/stage9.4.2-pre-bootstrap-memo.md` line 112 verbatim: *"1. **`praxis` is now a 10-path namespace package.** `praxis.__path__` aggregates `adapters/{beads,tonl}/src/praxis`, `kernel/{compression,mac,memory,pi-mono,runtime,studio}/src/praxis`, `ports/src/praxis`, `tests/src/praxis`, plus the pi-mono importlib-finder hook entry. PEP-420 namespace resolution working post-Finding-C.2 marker deletions."* The memo's "10-path" label counts only the `src/praxis` paths and treats the pi-mono importlib-finder hook entry as additive ("plus the … hook entry"); recount-from-list: 2 (`adapters/{beads,tonl}`) + 6 (`kernel/{compression,mac,memory,pi-mono,runtime,studio}`) + 1 (`ports`) + 1 (`tests`) = 10 `src/praxis` paths, + 1 importlib-finder hook entry = 11 total `praxis.__path__` entries at runtime. Halt-point #1 indexed enumeration of `praxis.__path__` returned `len = 11` with the 10 `src/praxis` paths at indices 0, 1, 2, 3, 4, 5, 6, 8, 9, 10 and the pi-mono importlib-finder hook entry at index 7 — set-equality with the memo's enumeration. The handover's "10-element" wording conflated the memo's "10-path" label (which counts `src/praxis` paths only) with the total `len(praxis.__path__)` value (which includes the additive importlib-finder hook entry); the runtime aggregate is `len = 11`, not `10`, while still matching the memo verbatim at the membership-set level.

**Disposition.** Classified — non-defect paraphrase drift, F6-precedent shape. F6 at `_bmad-output/planning-artifacts/Verdaca/session-handoff-20260430-stage9.4.2-pre-close.md` §3.1 line 49 disposition first-sentence verbatim: *"**Disposition.** Charter-shorthand-vs-actual-location mismatch — same shape as F3 (`where=["api"]` charter shorthand → actual `where=["."]+include=["api*"]`)."* F14 is the same handover-paraphrase-vs-binding-source-mismatch class: handover paraphrase ("10-element") vs binding source enumeration ("10-path … plus the pi-mono importlib-finder hook entry"). No on-disk state divergence — runtime state matches the memo's enumeration at the membership-set level (10 `src/praxis` paths + 1 pi-mono importlib-finder hook entry); the indexed-enumeration evidence at halt-point #1 confirmed this and step 7 proceeded without halt-class escalation. No corrigendum required: the handover's wording is the drift locus, not the runtime state.

**Forward action.** Future preload checks cite the binding source's enumeration verbatim (e.g., a memo-§5-style "10-path namespace package … plus the pi-mono importlib-finder hook entry" block, not a "10-element" paraphrase of the aggregate `len()` value). Future close-handoff Item-N wording authored at handover-time verifies any paraphrase against the binding source it refers to before landing in handover prose — F6's forward action codified the same discipline at the cite-pinning level (`session-handoff-20260430-stage9.4.2-pre-close.md` §3.1 line 51 verbatim fragment: *"**Forward action.** Future charters cite `playbook addenda §9.C (04-26 handoff lines 199–203, operationalized at 04-28 lines 59–66)`, not `test-strategy.md §9.C`."*). The F6 + F14 pair signals a recurring drift class (handover-time paraphrase of binding-source enumerations); cite-pinning discipline already standing per `feedback_provenance_pin.md` precedent shape covers it without a new memory-class entry. No `Underlying gap:` subordinated clause — F14 is a one-shot drift instance, not a systemic gap; the systemic discipline is already captured at F6 and the standing memory ledger.

---

## §4. Parked items roll-forward

- **F9** (Windows case-fold `.gitignore` latent landmine) — unchanged from `session-handoff-20260430-stage9.4.2-pre-close.md` §3.4 line 71: open-deferred separate-session audit. No new instances surfaced during step 7 preload (halt-point #1 verification).
- **F10** (`praxis.kernel` package-shape asymmetry) — unchanged from `session-handoff-20260430-stage9.4.2-pre-close.md` §3.5 line 79: open-deferred Stage 9.5 architectural review scope. F10 caveat surfaced verbatim in the executor handover line 56 ("Note F10 caveat in close-handoff §3.5 about `praxis.kernel` asymmetry — surface but do NOT auto-resolve.") and was duly surfaced at halt-point #1 without auto-resolution attempt.
- **`docs/pipeline-stage9.md`** — pre-existing modification still parked at HEAD `1ea7ac6` per halt-point #5 §B working-tree record (` M docs/pipeline-stage9.md` only; not staged, not modified during step 7). Disposition unchanged from `session-handoff-20260430-stage9.4.2-pre-close.md` §6 line 117 onward: defer to separate session for git-archaeology.
- **`feedback_pre_stamp_reproducibility_audit.md` candidate** — unchanged from `session-handoff-20260430-stage9.4.2-pre-close.md` §5 line 102 onward (cross-referenced at §6 line 122: *"see §5 above; parked for next charter-close session"*): parked future memory candidate, reserved for next charter-close session. NOT authored at this close-handoff per `feedback_memory_authorization.md` discipline (no agent-initiative memory writes; team-lead disposition required for any memory-class entry).
- **Transient-12 first-import `len()` ambiguity** — informational carry-forward from halt-point #1 of the executor session (single `len()` call on the first `praxis.__path__` import returned 12; indexed re-read returned 11 stable). Advisor disposition at halt-point #1: one-shot non-blocking; do-not-investigate-under-step-7. Not in `session-handoff-20260430-stage9.4.2-pre-close.md` §6 (post-dates that close-handoff); recorded here for forward visibility without re-opening.

---

## §5. Provenance pin

Per `feedback_provenance_pin.md` + `feedback_session_surface_audit.md`. Populated against actual session surface; fields verified empirically at §5 surface.

| Field | Value |
|---|---|
| Surface | Claude Code CLI (executor role) |
| Model | `claude-opus-4-7[1m]` |
| Working dir | `C:\Users\AndreyPopov\Documents\Anthropic` |
| Local date | 2026-05-01 |
| HEAD at write | `1ea7ac66b4d2677cacaf9699637ec308d1be3ab8` (short form `1ea7ac6`) — step-7 substance-close commit (parent of the close-handoff commit). §5 records parent SHA only; no self-reference — the close-handoff's own commit SHA will be assigned at halt-point #7 commit and is not recorded in §5. The no-self-reference principle for close-time provenance pins is established at `session-handoff-20260430-stage9.4.2-pre-close.md` §4 (lines 89–98). |
| Lockfile sha256 | `a3362cb3cd8d0a5e8fa39f920b3639cb4a2befa7fb2ca12e78ae0b3b28d119cb` (verified at §5 surface against current `uv.lock`; unchanged from `stage9.4.2-pre-bootstrap-memo.md` §6 record + `session-handoff-20260501-stage9.4.2-internal-executor.md` line 54). |
| Executor session JSONL | `~/.claude/projects/C--Users-AndreyPopov-Documents-Anthropic/a01df764-26f9-4aba-9ffc-5e1dfacbbb4c.jsonl` (most-recently-modified at §5 surface; same JSONL declared at halt-point #1 §1 surface declaration). |
| Companion advisor JSONL | **Unverified at halt-point #7 close.** Candidate value `~/.claude/projects/C--Users-AndreyPopov-Documents-Anthropic/5f86b89b-dc88-4c94-acf7-38a2af08004e.jsonl` was proposed at §5 surface as the second-most-recently-modified JSONL at executor surface time (with the executor session's `a01df764-…` as the most-recently-modified). Team-lead Andrey's halt-point #7 routing-layer verification returned unverifiable; the candidate is recorded for forensic context but the advisor-JSONL pairing is NOT confirmed at this close. |
| Parallel-session shape | Executor (this session, JSONL above) and advisor (parallel session, companion JSONL above) run concurrently with team-lead Andrey routing between roles per `session-handoff-20260501-stage9.4.2-internal-executor.md` line 7 verbatim: *"The advisor (Claude Opus 4.7 1M context) is in a parallel session. The team-lead (Andrey) routes messages. You report to the advisor, halt-and-surface at all halt points, and never mutate state without explicit advisor authorization."* Companion handover artifact: `session-handoff-20260501-stage9.4.2-internal-advisor.md` (per executor handover line 86 footer reference). |

---

## §6. Handoff to next scope

**Predicate state at HEAD `1ea7ac6`** (post-step-7 substance close, pre-close-handoff commit). 12-MAC-T Serialization port contract test catalog tracked + green; pytest baseline at `tests/src/praxis/contract_tests/ports/` = 22 collected / 20 passed / 2 skipped / 2 warnings (halt-point #5 §D record). §9.C runtime contract holds (halt-point #5 verification: `isinstance(TONLAdapter(), SerializationPort) == True`). No collateral damage to the 10-test `test_versioned_state_contract.py` precedent. Working tree clean except for the parked `docs/pipeline-stage9.md` modification.

**Out of scope for this close-handoff.** The §4 parked items (F9, F10, `docs/pipeline-stage9.md`, `feedback_pre_stamp_reproducibility_audit.md` candidate, Transient-12) remain parked under their original deferral targets and are not re-opened here. F11 / F12 / F13 coupled landing belongs to "the next Amelia stage that opens pytest-config / meta-test scope" per the §3.1 / §3.2 / §3.3 Forward action sections; that stage's identity is not pre-decided here. F14 is classified-and-closed within this close-handoff (paraphrase-drift forward-action discipline standing per `feedback_provenance_pin.md` shape; no follow-up artifact required).

**Stamp-shape open question — halt-point #7 disposition.** Per advisor halt-point #6 §C §2-reframe directive, this §6 carries forward the close-handoff-commit-shape question that §2's branch-state line marked as deferred (`04a4992 → 1ea7ac6 (close-handoff commit shape pending halt-point #7 disposition)`). The substantive open question: whether step 7's close-handoff lands as **(a)** a single close-handoff commit on top of `1ea7ac6` (close-handoff text is itself the close artifact; no separate stamp commit follows), or **(b)** a substance + stamp two-commit shape mirroring the 9.4.2-pre / 9.4.2-pre.1 precedent (close-handoff commit lands the artifact text; a follow-up stamp commit backfills any post-commit-known fields). Executor's supporting context (NOT a pre-decision): step 7 has no separate close memo (unlike the 9.4.2-pre case, which had `stage9.4.2-pre-bootstrap-memo.md` as a distinct artifact whose §6 SHAs needed self-reference resolution via a paired stamp commit); the close-handoff's §5 provenance pin records HEAD-at-write = `1ea7ac6` (parent SHA only) and does not contain any field unknowable at write-time. This reasoning suggests shape (a) may be sufficient for the step-7 case, but the architectural call belongs to halt-point #7 advisor + team-lead adjudication; §6 surfaces the question without pre-deciding it.

**Next-scope pointer.** Stage 9.4.2-internal status post-step-7 (whether further steps exist for additional port catalogs vs end-of-stage closure) is not pinned in the executor handover. Remaining `test-strategy.md` v0.2 §2.2 port catalogs beyond Serialization — Memory at §2.2.1 (18 MAC-Ts), Compaction at §2.2.4 (14 MAC-Ts), Cost Meter at §2.2.5 (8 MAC-Ts), LLM Proxy at §2.2.6 (10 MAC-Ts), matrix harness at §2.2.7 (6 base + 3 substitute) — are unauthored at HEAD `1ea7ac6` (verified at §6 surface: `tests/src/praxis/contract_tests/ports/` contains only `test_serialization_contract.py` + `test_versioned_state_contract.py`); their stage-assignment is not within this close-handoff's scope to declare. Stage 9.5 is referenced as the F10 architectural review target. "Next Amelia stage" in the F11 / F12 / F13 Forward actions is the canonical authoring target for the coupled meta-test + pytest-config landing. Halt-point #7 commit gate + advisor adjudication closes this close-handoff scope.

---

*Authored 2026-05-01 by Claude Code CLI session (model `claude-opus-4-7[1m]`, JSONL `a01df764-26f9-4aba-9ffc-5e1dfacbbb4c`); HEAD at write `1ea7ac6`.*

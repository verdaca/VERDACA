{# provenance_invisible.j2 — ADR-09 invisible mode.
   Empty — no Studio identifiers in rendered text, file metadata, or HTML comments.
   STUDIO-T-TPL-PROV-03: renders no provenance content.
   STUDIO-T-PROV-LEAK-01 through LEAK-04: byte-level grep for praxis/studio = 0 matches.
   The Python renderer's _apply_provenance_strip() handles content-level strip.
#}

# missing_name.yaml — triggers STUDIO-T-SCHEMA-NEG-01
# Missing required field: name
product: studio
description: "A template missing the name field"
version: "0.1.0"
rendering_mode: "position_to_hold"
inputs:
  strategic_question:
    type: string
    required: true
    description: "The question"
cycles:
  - name: wide_survey
    budget_pct: 100
    agents:
      - role: researcher
        agent_id: bmad_mary
        output_label: research_output
        depends_on: []
quality_gates:
  - gate_id: R1
    min_score: 3
outputs:
  - format: markdown
    template_path: "position_to_hold/brief.md.j2"
    rendering_mode: "position_to_hold"
cost_budget_usd: 5.0
timeout_seconds: 300

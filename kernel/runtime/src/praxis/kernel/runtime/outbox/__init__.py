"""Praxis Runtime — CostEvent outbox integration (F-1 absorption).

Architecture §8.1: Path A (reaper-direct same-transaction) and Path B
(tick-drain) emission of Memory CostEvents into Pi-Mono's events_outbox.
"""

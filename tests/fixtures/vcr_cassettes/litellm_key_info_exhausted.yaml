# Synthetic LiteLLM key-info cassette for Stage 12 H#4.
# Security-relevant: spend equals max_budget and must block budget checks.
interactions:
  - request:
      method: GET
      uri: https://litellm.example.invalid/key/info?key_alias=stage12-key
    response:
      status:
        code: 200
      headers:
        content-type: application/json
      body:
        string: |
          {"info":{"token":"sk-stage12-existing","spend":25.0,"max_budget":25.0}}

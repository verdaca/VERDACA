# Synthetic LiteLLM key-list error cassette for Stage 12 H#4.
interactions:
  - request:
      method: GET
      uri: https://litellm.example.invalid/key/list?key_alias=stage12-key&return_full_object=true
    response:
      status:
        code: 500
      headers:
        content-type: application/json
      body:
        string: |
          {"error":"synthetic proxy failure"}

# Synthetic LiteLLM key-info error cassette for Stage 12 H#4.
interactions:
  - request:
      method: GET
      uri: https://litellm.example.invalid/key/info?key_alias=stage12-key
    response:
      status:
        code: 500
      headers:
        content-type: application/json
      body:
        string: |
          {"error":"synthetic proxy failure"}
